class TimerCardConfiguration {
  var isWithMargin;
  var isWithBorderRadius;
  // ignore: empty_constructor_bodies
  TimerCardConfiguration({bool isWithMargin, bool isWithBorderRadius}) {
    this.isWithMargin = isWithMargin;
    this.isWithBorderRadius = isWithBorderRadius;
  }
}
